#!/usr/bin/env sloth

let includePath = "${ $scriptDir }/include"
let src = ("# https://clangd.llvm.org/config
CompileFlags:
  Add: [-Wall, -Werror, -Wextra, -Wpedantic, -I${includePath}]
")

src -> "${ $scriptDir }/.clangd"
